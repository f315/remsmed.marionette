define([
    'jquery',
    'underscore',
    'backbone'
],function($, _, Backbone){
    'use strict';

    var WblockModel = Backbone.Model.extend({
        defaults: {

        }
    });

    return WblockModel;
})